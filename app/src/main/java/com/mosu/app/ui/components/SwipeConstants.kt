package com.mosu.app.ui.components

/**
 * Global swipe threshold configuration
 */
object SwipeThresholds {
    const val ADD_TO_PLAYLIST = 0.3f  // 40% progress required for add actions
    const val DELETE = 0.5f           // 70% progress required for delete actions
}
